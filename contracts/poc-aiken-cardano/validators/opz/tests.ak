use cardano/transaction.{Transaction}
use opz/opz

const owner = mock_pub_key_hash(-),

fn mock_tx_with_sign_owner() {
  Transaction { ..placeholder, extra_signatories: extra_signatories }
}

fn mock_tx_without_sign_owner() {
  todo
}

test is_owner() {
  opz.isOwner()
}

test is_not_owner() {
  opz.isOwner()
}
